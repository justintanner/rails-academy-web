server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /up {
        return 200 'OK';
        add_header Content-Type text/plain;
    }
}
